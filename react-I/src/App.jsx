const textDesc = ['Fundamental', 'Crucial', 'Core', 'Rudimentary'];

const getRandom = () => { 
  return Math.floor(4 * Math.random());
};

const Header = () => {
  return (
    <header>
      <img src="src/assets/react-core-concepts.png" alt="Stylized atom" />
      <h1>React Essentials</h1>
      <p>
        {textDesc[getRandom()]} React concepts you will need for almost any app you are
        going to build!
      </p>
    </header>
  );
}

function App() {
  return (
    <div>
      <Header />
      <main>
        <h2>Time to get started!</h2>
      </main>
    </div>
  );
}

export default App;
